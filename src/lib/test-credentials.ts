import type { LoginRequest } from '@/lib/types.ts';

// These public credentials belong only to the case-study test API.
export const TEST_CREDENTIALS: LoginRequest = {
	email: 'frontend@nfctron.com',
	password: 'Nfctron2025',
};